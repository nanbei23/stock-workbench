# 分批加仓盯盘工作站 PRD

> 基于用户已有 demo（index.html v1.1 + 经典版）升级为全功能 Web 应用

---

## 1. 产品定位

**一句话**：A股分批加仓策略的实时盯盘工具——自动行情、智能建议、新闻聚合、盈亏追踪。

**目标用户**：自己用（个人交易者），需要同时监控 5-50 只股票的分批建仓/减仓计划。

**核心价值**：
- 不再手动查价格，自动刷新行情
- 不再手动算加仓价，策略引擎自动计算下一笔触发价
- 不再漏看公告，自动抓取相关新闻并标注情绪
- 不再算不清盈亏，实时计算含手续费的浮盈/浮亏

---

## 2. 功能模块

### 2.1 自选股管理（左侧栏）

| 功能 | 描述 |
|------|------|
| 多标的槽位 | 最多 50 个自选股槽位，每个独立维护策略参数 |
| 拖拽排序 | 拖拽卡片调整顺序，松手自动保存 |
| 行情卡片 | 显示：代码、名称、现价、涨跌幅、浮盈/亏 |
| 涨跌色条 | 卡片顶部色条：红涨绿跌灰平 |
| 快速切换 | 点击卡片切换主视图，当前选中高亮 |
| 添加/删除 | 支持输入代码添加，长按或按钮删除 |

### 2.2 行情大卡（Hero 区）

| 功能 | 描述 |
|------|------|
| 实时价格 | 大字显示现价，自动刷新（默认 10 秒） |
| 涨跌幅 | 百分比 + 绝对值，红涨绿跌 |
| 策略信号 | 根据现价 vs 计划价显示：🟢加仓区 / 🟡观望区 / 🔴减仓区 |
| 关键价位 | 显示下一笔加仓价、减仓价、距当前价百分比 |

### 2.3 K 线图 + MACD

| 功能 | 描述 |
|------|------|
| 多周期 | 日K / 周K / 月K / 1分钟 / 5分钟 / 15分钟 / 30分钟 / 60分钟 |
| 数据源 | 东方财富 JSONP API（主）+ 腾讯财经（备） |
| MACD 副图 | DIF / DEA / 柱状图，与主图联动 |
| 实时更新 | 分钟级 K 线自动刷新 |
| 技术指标 | EMA(12/26) 计算，MACD(12,26,9) |

### 2.4 分批加仓策略引擎

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 总预算 | 可投入总金额 | 手动输入 |
| 首笔买入价 | 建仓价格 | 手动输入 |
| 跌幅加仓% | 每跌多少加仓 | 5% |
| 加仓倍数 | 每次加仓金额倍数 | 1.5x |
| 反弹减仓% | 涨多少减半仓 | 8% |
| 手数 | 每笔最小交易单位 | 100 股 |
| 目标净利% | 止盈目标收益率 | 5% |

**策略逻辑**：
```
第1笔: 首笔价 × 手数
第2笔: 首笔价 × (1 - 跌幅%) × 手数 × 倍数
第3笔: 首笔价 × (1 - 跌幅%×2) × 手数 × 倍数²
...
减仓: 当前价 × (1 + 反弹%) 触发减半仓
止盈: 反推卖出价 = (总成本 × (1 + 目标%)) / 总股数
```

### 2.5 成交记录 + 持仓快照

| 功能 | 描述 |
|------|------|
| 手动记录 | 记录每笔买入/卖出（价格、股数、日期） |
| 持仓汇总 | 自动计算：总股数、均价、总成本 |
| 浮盈/亏 | 实时计算：(现价 - 均价) × 总股数 |
| 费用计算 | 佣金(万2.5) + 印花税(千1, 卖出) + 过户费(十万分之一) |
| 净盈亏 | 扣除全部费用后的实际盈亏 |
| 撤销 | 支持撤销最近一笔交易 |
| 清空 | 清空所有记录（需确认） |

### 2.6 实时买卖建议

| 功能 | 描述 |
|------|------|
| 自动建议 | 根据现价 vs 计划价，实时给出"加仓/减仓/观望"建议 |
| 建议历史 | 记录每次建议，可回溯 |
| 价格预警 | 现价触及关键价位时弹出提醒 + 浏览器通知 |
| 自定义买点 | 手动标记额外关注价位 |

### 2.7 新闻聚合

| 功能 | 描述 |
|------|------|
| 多源抓取 | 东方财富公告 + Google News RSS + 自定义 RSS |
| 情绪标注 | 自动分析新闻情绪：偏多/偏空/中性 |
| 影响评级 | 利好/利空/中性 标签 |
| 高风险预警 | 退市/立案/处罚等关键词自动标红 |
| 分类筛选 | 按情绪/影响分类筛选 |
| 分页浏览 | 支持分页 + 每页条数选择 |
| 关键词过滤 | 自定义关键词过滤新闻 |
| 导出 | 导出为 Markdown 格式 |

---

## 3. 技术方案

### 3.1 架构

```
┌─────────────────────────────────────────────┐
│                  浏览器                       │
│  ┌─────────┐  ┌──────────┐  ┌─────────────┐ │
│  │ 自选栏   │  │ K线+行情  │  │ 策略+新闻   │ │
│  │ (Vue3)   │  │ (LWC4.2) │  │ (Vanilla)   │ │
│  └────┬─────┘  └────┬─────┘  └──────┬──────┘ │
│       │              │               │        │
│  ┌────┴──────────────┴───────────────┴──────┐ │
│  │           共享状态 (localStorage)          │ │
│  └──────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
         │                    │
    东方财富API           RSS/Google News
    (JSONP/CORS)          (rss2json代理)
```

### 3.2 数据存储

**localStorage**，key = `stock-strategy-tracker-v2`：

```json
{
  "settings": {
    "stockCode": "002470",
    "stockName": "金正大",
    "budget": 100000,
    "entryPrice": 3.5,
    "dropPct": 5,
    "addMult": 1.5,
    "bouncePct": 8,
    "lotSize": 100,
    "targetProfitPct": 5,
    "refreshSec": 10,
    "useManualPrice": false,
    "newsListFilter": "all",
    "newsListPage": 1,
    "newsListPageSize": 20,
    "newsKeywords": "",
    "newsSector": "",
    "newsWindowDays": 7,
    "rss2jsonKey": ""
  },
  "ledger": [
    { "id": "uuid", "type": "buy", "price": 3.5, "shares": 1000, "date": "2024-01-15" }
  ],
  "buyPoints": [
    { "id": "uuid", "price": 3.2, "note": "支撑位" }
  ],
  "adviceHistory": [
    { "ts": 1700000000000, "price": 3.45, "suggestion": "观望", "note": "距下一加仓价约5%" }
  ],
  "newsItems": [
    { "id": "hash", "title": "...", "content": "...", "link": "...", "date": "...", "source": "...", "stockImpact": "利好", "sentiment": "偏多" }
  ],
  "watchlist": [
    { "code": "002470", "name": "金正大" },
    { "code": "601138", "name": "工业富联" }
  ],
  "activeWatchIndex": 0
}
```

### 3.3 外部 API

| 用途 | API | 格式 |
|------|-----|------|
| 实时行情 | `https://push2.eastmoney.com/api/qt/stock/get?secid={market}.{code}&fields=f43,f44,f45,f46,f47,f48,f50,f51,f52,f170` | JSON |
| K 线数据 | `https://push2his.eastmoney.com/api/qt/stock/kline/get?secid={market}.{code}&klt={period}&fqt=1&lmt=200&end=20500101&fields1=f1,f2,f3,f4,f5,f6&fields2=f51,f52,f53,f54,f55,f56,f57` | JSON |
| 公告 | `https://np-anotice-stock.eastmoney.com/api/security/ann?sr=-1&page_size=50&page_index=1&ann_type=SHA,SZA&client_source=web&stock_list={code}` | JSON |
| 新闻 RSS | Google News RSS → rss2json.com 代理 | JSON |
| 自选股行情 | `https://push2.eastmoney.com/api/qt/ulist.np/get?fields=f2,f3,f12,f14&secids={list}` | JSON |

### 3.4 前端技术栈

| 组件 | 技术 |
|------|------|
| 框架 | Vanilla JS（单文件，无构建） |
| 图表 | Lightweight Charts v4.2.0（CDN） |
| 样式 | 纯 CSS，暗色主题，CSS 变量 |
| 持久化 | localStorage |
| 通知 | Notification API（浏览器原生） |

---

## 4. UI 设计规范

### 4.1 色彩系统

```css
:root {
  --bg: #080b10;           /* 页面背景 */
  --panel: #121826;         /* 面板背景 */
  --panel2: #0e131c;        /* 次级面板 */
  --border: #243044;        /* 边框 */
  --text: #f0f4fa;          /* 主文字 */
  --muted: #7a8aa0;         /* 次要文字 */
  --accent: #5eb0ff;        /* 强调色（蓝） */
  --buy: #3ddc84;           /* 买入/涨（绿） */
  --sell: #ffb020;          /* 卖出（橙） */
  --warn: #ff5c5c;          /* 警告/跌（红） */
}
```

> **注意**：A股红涨绿跌，但 demo 中用绿=买入区、红=警告，两者语义不同。最终显示需统一。

### 4.2 布局

```
┌──────────────────────────────────────────────┐
│  ┌─────────┐  ┌────────────────────────────┐ │
│  │ 自选股   │  │  行情大卡 (Hero)           │ │
│  │ 卡片列表 │  │  代码 名称 现价 涨跌幅      │ │
│  │          │  ├────────────────────────────┤ │
│  │ [金正大] │  │  K 线图 + MACD 副图         │ │
│  │ [工业富联]│  │  [日K] [周K] [月K] [1分]   │ │
│  │ [均胜电子]│  ├────────────────────────────┤ │
│  │  ...     │  │  策略参数 / 计划表          │ │
│  │          │  │  费用盈亏 / 买卖建议        │ │
│  │          │  ├────────────────────────────┤ │
│  │          │  │  成交记录 / 新闻聚合        │ │
│  └─────────┘  └────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

- 左侧栏：300px 固定宽，sticky 定位
- 右侧主区：flex: 1，自适应
- 移动端：左侧栏折叠为顶部横向滚动

---

## 5. 与现有 stock-workbench 的关系

| 维度 | stock-workbench | 本工具 |
|------|----------------|--------|
| 定位 | AI 分析 + 多角色辩论 | 分批加仓策略盯盘 |
| 数据源 | mootdx + 腾讯 + 东财 | 东财 JSONP（纯前端） |
| 后端 | FastAPI + SQLite | 无后端（纯前端） |
| 部署 | 本地 python app.py | 单 HTML 文件，浏览器直接打开 |

**建议**：本工具作为 stock-workbench 的**子页面**集成，路径 `/watchlist`，共享自选股数据库。

---

## 6. 开发计划

| 阶段 | 内容 | 工作量 |
|------|------|--------|
| P0 | 合并两个 demo 为统一代码库，修复已知 bug | 2h |
| P1 | 接入 stock-workbench 后端 API（共享自选股） | 3h |
| P2 | 优化移动端适配 + 暗色主题统一 | 2h |
| P3 | 接入 AI 分析台（一键分析当前选中股票） | 2h |
| P4 | 新闻聚合升级（接入后端新闻 API） | 2h |

---

## 7. 已知问题（Demo 中）

1. **行情刷新频率**：默认 10 秒，但东财 API 有频率限制，需加缓存
2. **K 线数据源**：QQ K 线接口不稳定，需切换为东财 JSONP
3. **新闻去重**：基于 title hash，但同标题不同日期的新闻会丢失
4. **费用计算**：佣金万2.5 是默认值，应可配置
5. **localStorage 容量**：50 个标的的新闻数据可能超 5MB 限制
6. **macOS 12.7.6 兼容**：部分 CSS 特性（如 `backdrop-filter`）在 Safari 15 上需降级
